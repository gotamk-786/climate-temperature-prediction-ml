# External Data

This folder is reserved for optional supporting datasets.

Examples:
- NASA GISTEMP reference files
- NOAA backup dataset files
- any external climate-support CSV files used for comparison only

The main project dataset remains in `data/raw/`.
